# Multi-Modal Evidence Review — dependencies
#
# CSV parsing uses the stdlib. The VLM pipeline needs:
Pillow>=11.3        # image decode/downscale/probe; >=11.3 decodes AVIF natively
anthropic>=0.69     # Claude VLM backend (required — the system is VLM-only)
