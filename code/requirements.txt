# Multi-Modal Evidence Review — dependencies
# CSV parsing uses the stdlib. Pillow >= 11.3 decodes AVIF natively, which the
# dataset needs because several files are AVIF/WEBP mislabeled as .jpg.
Pillow>=11.3
