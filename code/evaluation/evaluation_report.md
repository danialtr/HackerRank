# Evaluation Report — Multi-Modal Evidence Review

Scored on `dataset/sample_claims.csv` (20 labeled claims). Backend: **heuristic backend (no API key — deterministic CV + claim-text parsing)**.

We score the 10 predicted columns: exact (normalised) match for the scalar
columns, and set-overlap (Jaccard) for the two list columns
(`risk_flags`, `supporting_image_ids`). The headline metric is the
`claim_status` 3-class macro-F1.

## Strategy comparison (the required ≥2-approach ablation)

| strategy | overall | claim_status macro-F1 | est. test-set cost |
|---|---|---|---|
| Multi-stage pipeline (heuristic) | 0.656 | 0.267 | $0.0000 |
| Trivial claim-echo baseline (no perception) | 0.494 | 0.250 | $0.0000 |

**Selected for `output.csv`: Multi-stage pipeline (heuristic)** — the multi-stage pipeline keeps the
deterministic decision logic (precedence rule, enum compliance) in code and uses
the model only for perception, which is more debuggable and cheaper per unit of
accuracy than the single mega-prompt. (Winner by overall score above: Multi-stage pipeline (heuristic).)

### Multi-stage pipeline (heuristic)

Overall score (mean of scored columns): **0.656**
claim_status macro-F1: **0.267**

| column | score |
|---|---|
| evidence_standard_met | 0.750 |
| issue_type | 0.450 |
| object_part | 0.650 |
| claim_status | 0.600 |
| valid_image | 0.900 |
| severity | 0.450 |
| risk_flags (Jaccard) | 0.674 |
| supporting_image_ids (Jaccard) | 0.775 |

claim_status confusion matrix (rows = gold, cols = predicted):

```
gold\pred     sup    con    nei
      sup      12      0      0
      con       3      0      2
      nei       3      0      0
```

Operational: 0 model calls, 0+0 in/out tokens, $0.0000 on the sample; ~$0.00000/claim, est. **$0.0000** for the full test set (0 calls); runtime 0.49s.

### Trivial claim-echo baseline (no perception)

Overall score (mean of scored columns): **0.494**
claim_status macro-F1: **0.250**

| column | score |
|---|---|
| evidence_standard_met | 0.850 |
| issue_type | 0.150 |
| object_part | 0.050 |
| claim_status | 0.600 |
| valid_image | 0.900 |
| severity | 0.150 |
| risk_flags (Jaccard) | 0.500 |
| supporting_image_ids (Jaccard) | 0.750 |

claim_status confusion matrix (rows = gold, cols = predicted):

```
gold\pred     sup    con    nei
      sup      12      0      0
      con       5      0      0
      nei       3      0      0
```

Operational: 0 model calls, 0+0 in/out tokens, $0.0000 on the sample; ~$0.00000/claim, est. **$0.0000** for the full test set (0 calls); runtime 0.0s.


## Operational analysis

- **Model calls.** Pipeline: 1 cheap text call (claim extraction) + 1 vision
  call per image + an occasional Opus tie-breaker on ambiguous claims. With ~1.8
  images/claim that is roughly 3 calls/claim. The mega-prompt uses 1 call/claim
  but loads every image into one context.
- **Tokens & images.** See per-strategy lines above. Images are downscaled to a
  ~1568px long edge before encoding (vision tokens scale with resolution), which
  is the single biggest token saver.
- **Cost.** Per-claim and extrapolated full-test-set cost are shown per strategy,
  computed from live `usage` and the Claude API price table in `config.PRICING`
  (Opus 4.8 $5/$25, Sonnet 4.6 $3/$15, Haiku 4.5 $1/$5 per 1M in/out).
- **Caching.** The large static perception instruction block is sent as a cached
  system block, so repeated per-image calls pay cache-read (~0.1x), not full
  input price.
- **Rate limits / retries.** The Anthropic SDK retries 429/5xx with exponential
  backoff (`max_retries=5`); identical images are de-duplicated by the loader and
  could be processed concurrently for higher throughput.
- **Determinism.** The decision logic, evidence lookup, history rules, and schema
  normalisation are pure code, so re-runs on the same perception are identical.

_Generated by `code/evaluation/main.py`._
